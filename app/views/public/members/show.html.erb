<div class="title">マイページ</div>

<h5>登録情報 <%= link_to "編集する", edit_member_path, class: "btn-sm btn-primary" %>  <%= link_to "パスワードを変更する", edit_member_password_path, class: "btn-sm btn-primary" %></h5>

<table border="1">
    <tr>
      <td>氏名</td>
      <td><%= @member.first_name %> <%= @member.last_name %></td>
    </tr>
    <tr>
      <td>カナ</td>
      <td><%= @member.first_name_kana %> <%= @member.last_name_kana %></td>
    </tr>
    <tr>
      <td>郵便番号</td>
      <td><%= @member.postal_code %></td>
    </tr>
    <tr>
      <td>住所</td>
      <td><%= @member.address %></td>
    </tr>
    <tr>
      <td>電話番号</td>
      <td><%= @member.phone_number %></td>
    </tr>
    <tr>
      <td>メールアドレス</td>
      <td><%= @member.email %></td>
    </tr>
  </table>

  <h5>配送先 <%= link_to "一覧を見る", delivery_destinations_path, class: "btn-sm btn-primary" %></h5>
  <h5>注文履歴 <%= link_to "一覧を見る", orders_path, class: "btn-sm btn-primary" %></h5>